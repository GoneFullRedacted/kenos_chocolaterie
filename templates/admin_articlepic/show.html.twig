{% extends 'base.html.twig' %}

{% block title %}Articlepic{% endblock %}

{% block body %}
    <h1>Articlepic</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ articlepic.id }}</td>
            </tr>
            <tr>
                <th>Pic</th>
                <td>{{ articlepic.pic }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_articlepic_index') }}">back to list</a>

    <a href="{{ path('app_admin_articlepic_edit', {'id': articlepic.id}) }}">edit</a>

    {{ include('admin_articlepic/_delete_form.html.twig') }}
{% endblock %}
