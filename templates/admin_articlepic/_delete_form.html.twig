<form method="post" action="{{ path('app_admin_articlepic_delete', {'id': articlepic.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ articlepic.id) }}">
    <button class="btn">Delete</button>
</form>
